/** REACT NATIVE **/
import React from 'react';

/** PROJECT FILES **/
import { 
  I18n,
} from '../../../Services/LibLinking';

/** NPM LIBRARIES **/

export default class SAPControllers extends React.Component {
  constructor(props){
    super(props);

    // Create modal object
  }

  initECommerceLandingScreen(){
    let result = new Promise((resolve, reject) => {
      // resolve(this.productCategory.FetchCategory());
    })
    return result;
  }
}
